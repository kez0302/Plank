services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: plank
      POSTGRES_PASSWORD: plank
      POSTGRES_DB: plank_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: